/**
 * Diagram engine — compiles Architecture IR into a DiagramScene and lays it
 * out under constraints (ELK adapter lands with issue #5).
 *
 * Placeholder module: DiagramScene type lands with issue #4. No pixel
 * coordinates are allowed in the Architecture IR; layout hints stay semantic.
 */
export const DIAGRAM_ENGINE_VERSION = "0.1.0";
