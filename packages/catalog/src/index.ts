/**
 * Catalog — model index, revision selection and search data.
 *
 * Placeholder module: catalog generation lands with issue #8. Consumes only
 * validated Architecture IR documents from models/ (never raw configs).
 */
export const CATALOG_VERSION = "0.1.0";
